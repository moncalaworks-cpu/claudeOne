# Claude Code Agentic Workflows - Environment Configuration Template
#
# Copy this file to .env and fill in your actual credentials
# .env is gitignored and will not be committed to version control
#
# Command to create your .env file:
#   cp .env.template .env
#
# Then edit .env with your actual credentials

# ═══════════════════════════════════════════════════════════════
# Anthropic API Configuration
# ═══════════════════════════════════════════════════════════════
# Get your API key from: https://console.anthropic.com/
# Required for all Claude Code agent operations

ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Model selection (optional, for explicit model specification)
# Default: claude-opus-4-1-20250805
# CLAUDE_MODEL=claude-opus-4-1-20250805

# ═══════════════════════════════════════════════════════════════
# Gmail MCP Configuration
# ═══════════════════════════════════════════════════════════════
# Setup instructions: See docs/Claude_Code_Agentic_Workflows_Guide.md Part 3
#
# To obtain credentials:
# 1. Go to https://console.cloud.google.com/
# 2. Create new project
# 3. Enable Gmail API
# 4. Create OAuth 2.0 credentials (Desktop application)
# 5. Download credentials.json and place in mcps/gmail_credentials.json
# 6. Run: claude-code --auth gmail
# 7. Copy the token path below

# Path to Gmail OAuth token (created after authentication)
GMAIL_TOKEN_PATH=./mcps/gmail_token.json

# Gmail configuration (optional)
# GMAIL_BATCH_SIZE=10
# GMAIL_MAX_RESULTS=50

# ═══════════════════════════════════════════════════════════════
# GitHub MCP Configuration
# ═══════════════════════════════════════════════════════════════
# Setup instructions: See docs/Claude_Code_Agentic_Workflows_Guide.md Part 4
#
# To obtain credentials:
# 1. Go to https://github.com/settings/tokens
# 2. Create Personal Access Token with scopes:
#    - repo (full control of private repositories)
#    - workflow (read/write access to GitHub Actions)
#    - read:repo_hook
# 3. Copy token below

GITHUB_TOKEN=your-github-personal-access-token-here

# GitHub configuration (optional)
# GITHUB_API_VERSION=2022-11-28
# GITHUB_REQUESTS_PER_HOUR=5000

# ═══════════════════════════════════════════════════════════════
# Logging Configuration
# ═══════════════════════════════════════════════════════════════
# Control agent logging levels and output

LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

LOG_FILE=logs/agent.log
# Location where logs are written

LOG_MAX_SIZE_MB=10
# Rotate log files at this size (in megabytes)

LOG_BACKUP_COUNT=5
# Keep this many old log files before deletion

# ═══════════════════════════════════════════════════════════════
# State Management Configuration
# ═══════════════════════════════════════════════════════════════
# Control how agents track persistent state

STATE_FILE=.agent-state.json
# Location where agent state is persisted

STATE_TTL_HOURS=24
# How long state is valid before expiration (0 = no expiration)

# ═══════════════════════════════════════════════════════════════
# Optional: Slack Integration
# ═══════════════════════════════════════════════════════════════
# For agents that send Slack notifications
# Get webhook from: https://api.slack.com/apps/

# SLACK_WEBHOOK_URL=your-slack-webhook-url-here
# SLACK_CHANNEL=#notifications
# SLACK_BOT_NAME=Claude-Agent

# ═══════════════════════════════════════════════════════════════
# Optional: Email Configuration (for agent notifications)
# ═══════════════════════════════════════════════════════════════

# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password-here
# NOTIFICATION_EMAIL=recipient@example.com

# ═══════════════════════════════════════════════════════════════
# Optional: Database Configuration
# ═══════════════════════════════════════════════════════════════
# If agents use database for state instead of files

# DATABASE_URL=sqlite:///./agent_state.db
# Or for PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost/agent_db

# ═══════════════════════════════════════════════════════════════
# Optional: Custom API Endpoints
# ═══════════════════════════════════════════════════════════════
# For agents integrating with custom internal APIs

# CUSTOM_API_URL=https://internal-api.example.com
# CUSTOM_API_KEY=your-api-key-here

# ═══════════════════════════════════════════════════════════════
# Deployment Environment
# ═══════════════════════════════════════════════════════════════
# Identify which environment this config is for

ENVIRONMENT=development
# Options: development, staging, production

# ═══════════════════════════════════════════════════════════════
# IMPORTANT SECURITY NOTES
# ═══════════════════════════════════════════════════════════════
#
# 1. NEVER commit .env to git
#    - It's already in .gitignore
#    - Verify with: git status
#
# 2. Rotate credentials regularly
#    - GitHub tokens: quarterly
#    - Gmail tokens: as needed
#    - API keys: based on org policy
#
# 3. Use minimal permissions
#    - GitHub PAT: only required scopes
#    - Gmail: only required scopes
#    - Custom APIs: read-only where possible
#
# 4. Store securely
#    - Development: .env file (gitignored)
#    - Staging: environment variables in secrets manager
#    - Production: credential manager (AWS Secrets, HashiCorp Vault, etc.)
#
# 5. Audit credential usage
#    - Enable audit logging in all systems
#    - Review logs regularly
#    - Alert on unusual activity
#
# 6. In emergencies
#    - Immediately revoke compromised credentials
#    - Rotate all related credentials
#    - Review audit logs for unauthorized access
#    - Follow incident response procedure
#
# ═══════════════════════════════════════════════════════════════

# Setup Instructions:
#
# 1. Copy this file:
#    cp .env.template .env
#
# 2. Edit .env with your actual credentials:
#    # Open in your editor
#    nano .env
#    # or
#    vim .env
#
# 3. Verify .env is gitignored:
#    cat .gitignore | grep ".env"
#    # Should show: .env
#
# 4. Test your configuration:
#    python -c "import os; from dotenv import load_dotenv; load_dotenv(); print('OK' if os.getenv('ANTHROPIC_API_KEY') else 'Missing ANTHROPIC_API_KEY')"
#
# 5. Start using your agent:
#    python agents/email_processor.py
